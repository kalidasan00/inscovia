import FeedClient from "./feed-client";

export const metadata = {
  title: "Feed | Insovia",
  description: "Notes, tips and updates from institutes, professors and students",
};

export default function FeedPage() {
  return <FeedClient />;
}